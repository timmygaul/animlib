animlib 0.4

This is a program that exports animation data from Maya to a .anim file and then allows you to extract and import that data back into Maya. For scenes with many rigs this can be much more convenient than importing the rigs using Maya’s default tools.